{
  "name": "openai-api-router",
  "version": "1.0.0",
  "description": "Cloudflare Worker to route OpenAI API requests to different endpoints",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test.js"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
} 